import { Typography } from '@mui/material'

interface IProps {
}

export function CustomFilters({ }: IProps) {

    return (
        <Typography fontWeight="bold">
            Ejemplo Filtros
        </Typography> // esto debe ser chidren
    )
}
